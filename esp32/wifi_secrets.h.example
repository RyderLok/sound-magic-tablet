// Copy to wifi_secrets.h (gitignored). Phone hotspot must be 2.4GHz.
#pragma once
#define WIFI_SSID "YOUR_HOTSPOT_NAME"
#define WIFI_PASS "YOUR_HOTSPOT_PASSWORD"
// Mac/PC IP on the same hotspot (empty = auto-find Python :8001 on LAN)
// Production (iPad independent): set to the iPad hotspot IP shown in PikoShell Settings.
#define WIFI_UPLOAD_HOST ""
#define WIFI_UPLOAD_PORT 8001
// Optional overrides (defaults in inmp441_bridge.ino):
// #define WIFI_DISABLE_USB_PCM 1   // Wi‑Fi-first: skip USB PCM stream
// #define WIFI_UPLOAD_MAX_ATTEMPTS 3
// #define WIFI_SCAN_LAST_OCTET 60
// #define WIFI_HTTP_PORT 8080      // GET /status  POST /record/start|stop
// Upload is async (button stays responsive). Prefer WIFI_UPLOAD_HOST = iPad (prod) or Mac (dev).
