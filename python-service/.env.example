# Optional SiliconFlow Qwen3-Omni open-ended audio semantic analysis.
# Copy to python-service/.env (gitignored) — never commit real keys.
# Used by POST /analyze/wav → response.semantic (does not change brushParams).

SILICONFLOW_API_KEY=
SILICONFLOW_OMNI_ENABLED=1
SILICONFLOW_OMNI_MODEL=Qwen/Qwen3-Omni-30B-A3B-Instruct
SILICONFLOW_BASE_URL=https://api.siliconflow.cn/v1

# When deploying (Docker / cloud), set the public HTTPS origin so /health links are correct.
# Example: PUBLIC_BASE_URL=https://piko-api.example.com
# Then put the same URL in web-demo/pikoCloudConfig.js → analysisBaseUrl
PUBLIC_BASE_URL=
HOST=0.0.0.0
PORT=8001
# Optional comma-separated extra CORS origins (app already allows *)
# CORS_ORIGINS=https://your-web-demo.example

# Supabase Sounds storage (service role — server only, never ship to frontend)
# If BOTH URL and SERVICE_ROLE_KEY are empty, uploads fall back to LOCAL disk:
#   <this-package>/sounds/*.wav  (+ index.json)
# On Mac with LaunchAgent that is usually:
#   ~/Library/Application Support/Piko/python-runtime/sounds/
# Check GET http://127.0.0.1:8001/health → supabase.localDir / supabase.hint
# After filling keys: sync runtime, restart :8001, then:
#   python migrate_local_to_supabase.py
#
# Desktop upload path (keep working): Bridge → Python :8001 → Supabase (service role).
# iPad App upload: Settings → Supabase URL + anon public key (never service_role).
# Run python-service/supabase_sounds.sql in SQL Editor (includes anon INSERT for iPad).
# iPad / browser direct READ: same anon key; optional fill web-demo/pikoCloudConfig.js
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_SOUNDS_BUCKET=sounds
# Optional: document the anon key you paste into iPad Settings (do not commit real keys)
# Used by: iPad LocalPikoGateway + python-service/verify_ipad_supabase.py
# SUPABASE_ANON_KEY=
# After changing policies, run SQL: python-service/supabase_sounds.sql
# Then: SUPABASE_ANON_KEY=... python3 verify_ipad_supabase.py

