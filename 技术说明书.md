# Piko — 产品技术说明书

**版本：** 1.3  
**更新日期：** 2026-07-15  
**项目路径：** `E:\项目四coding\项目四sound ai coding`

---

## 目录

1. [产品概述](#1-产品概述)
2. [目标用户与使用场景](#2-目标用户与使用场景)
3. [产品工作流](#3-产品工作流)
4. [系统架构](#4-系统架构)
5. [硬件与固件层](#5-硬件与固件层)
6. [Node.js 串口桥接层](#6-nodejs-串口桥接层)
7. [Web 前端应用](#7-web-前端应用)
8. [音频分析管线](#8-音频分析管线)
9. [视觉生成系统](#9-视觉生成系统)
10. [数据模型与接口协议](#10-数据模型与接口协议)
11. [项目文件结构](#11-项目文件结构)
12. [部署与启动](#12-部署与启动)
13. [技术栈](#13-技术栈)
14. [设计约束与原则](#14-设计约束与原则)
15. [已知限制](#15-已知限制)
16. [未来扩展路线](#16-未来扩展路线)
17. [修改记录（Changelog）](#17-修改记录changelog)

---

## 1. 产品概述

**Piko** 是一套面向自然情境的声音采集与生成式视觉系统。用户（预设 demo：儿童在自然环境中）发现一种声音 → 录制 → 停止 → 保存为独立样本 → 命名 → 系统将声音「理解」并转化为可绘制的**活体视觉材料（living material）**，最终在画布上用该材料进行创作。

产品核心理念：

> **声音不是被播放的对象，而是被转化的材料。**  
> Upload / Record → Analyze → Transform → Draw

系统由三部分协同构成：

| 层级 | 技术 | 职责 |
|------|------|------|
| 采集硬件 | ESP32 + INMP441 I2S 麦克风 | 现场拾音、实时音量指标 |
| 中间桥接 | Node.js Serial Bridge | USB 串口 → WebSocket 实时转发 |
| 交互前端 | HTML + p5.js + Web Audio API | 录音、分析、变换可视化、绘画 |

---

## 2. 目标用户与使用场景

### 2.1 预设 Demo 场景

儿童在自然环境中行走，每发现一种有趣声音（鸟鸣、流水、脚步声、风声等）：

1. 按下 **Start recording** 开始采集该瞬间
2. 听到/录到目标声音后 **Stop & save**
3. 为样本命名（如 `river`、`bird`），保存到 **Sound Library**
4. 继续寻找下一个声音，重复采集
5. 从 Library 选择样本 → **Transform** 观看透明化分析过程 → **Draw** 用转化后的材料绘画

### 2.2 与旧版差异

| 旧流程 | 当前流程 |
|--------|----------|
| 上传本地音频文件 | **现场录音**为主（一次开始→结束 = 一个样本） |
| 四步：Upload → Analyze → Transform → Draw | 三步：**Collect → Transform → Draw** |
| 无硬件联动 | ESP32 实时电平驱动可视化（可选） |

---

## 3. 产品工作流

```
┌─────────────────────────────────────────────────────────────────┐
│  ① Collect（采集）          Library View                         │
│  Start recording → 录音中 → Stop → 命名 → Save to library        │
└────────────────────────────┬────────────────────────────────────┘
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│  ② Transform（变换）        Transform View                       │
│  Step1 识别 → Step2 音量 → Step3 FFT → Step4 音色 →              │
│  Step5 映射 → Step6 Sound Breathing 预览                         │
└────────────────────────────┬────────────────────────────────────┘
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│  ③ Draw（绘制）             Analysis View                        │
│  左侧 Sound Breathing │ 中央画布 │ 右侧 AI 解读面板              │
│  鼠标拖拽 = 用活体材料绘画                                        │
└─────────────────────────────────────────────────────────────────┘
```

### 3.1 视图切换

| 视图 ID | 入口 | 说明 |
|---------|------|------|
| `libraryView` | 默认首页 | 录音 + 样本库 |
| `transformView` | 点击样本 / Transform | 透明化分析管线 |
| `analysisView` | Transform 完成后 Continue | 主创作画布 |

控制器：`web-demo/app.js`（全局 `App` 对象）

---

## 4. 系统架构

### 4.1 总体架构图

```
                    ┌──────────────────────┐
                    │   儿童 / 用户操作     │
                    │  浏览器 localhost:8000│
                    └──────────┬───────────┘
                               │
         ┌─────────────────────┼─────────────────────┐
         │                     │                     │
         ▼                     ▼                     ▼
  FieldRecorder          esp32AudioAdapter      SampleAnalyzer
  (浏览器麦克风录音)      (WebSocket 客户端)      (离线 FFT 分析)
         │                     │                     │
         │              ws://localhost:8765          │
         │                     │                     │
         │                     ▼                     │
         │            serial-bridge.js               │
         │                     │                     │
         │              USB Serial COM3              │
         │                     │                     │
         │                     ▼                     │
         │         ESP32 + INMP441 (sketch / .ino)   │
         └─────────────────────┴─────────────────────┘
                               │
                               ▼
              SoundPersonalityAI → VisualMappingEngine
                               │
                               ▼
         GenerativeField / BrushGenerator / CanvasInteraction
```

### 4.2 音频通道（v1.2）

| 通道 | 来源 | 用途 |
|------|------|------|
| **录制通道** | ESP32 INMP441 → Serial PCM → Bridge → WebSocket | Library 保存 **16kHz mono WAV** |
| **实时通道** | 同上（PCM 帧衍生 metrics） | 录音电平条 + Sound Breathing 实时反应 |

录音流程：`record_start` → ESP32 发 PCM 帧 → 前端缓冲 → `record_stop` → `encodeWavFromPcmBytes()` → Library。

> **必须**烧录 `inmp441_bridge.ino`（921600）并运行 `node serial-bridge.js --baud 921600`。

---

## 5. 硬件与固件层

### 5.1 硬件清单

| 组件 | 型号/规格 |
|------|-----------|
| 主控 | ESP32 Dev Module (ESP32-D0WD-V3) |
| 麦克风 | INMP441（I2S 数字麦克风） |
| 连接 | USB 转串口（如 Silicon Labs CP210x → COM3） |

### 5.2 INMP441 接线

| INMP441 引脚 | ESP32 |
|--------------|-------|
| SCK (BCLK) | GPIO 14 |
| WS (LRCLK) | GPIO 15 |
| SD | GPIO 32 |
| VDD | 3.3V |
| GND | GND |
| L/R | GND（左声道） |

### 5.3 音频参数

| 参数 | 值 |
|------|-----|
| 采样率 | 16000 Hz |
| I2S 位宽 | 32-bit 输入 |
| 有效位深 | 24-bit（`raw >> 8`） |
| 每帧样本数 | 512 |
| 串口波特率 | 115200（可升至 921600） |

### 5.4 固件文件

**推荐固件：** `esp32/inmp441_bridge.ino`

每帧计算并输出一行 JSON：

```json
{"level":12345,"peak":67890,"mean":-1234}
```

| 字段 | 含义 | 计算方式 |
|------|------|----------|
| `level` | RMS 能量 | `sqrt(sum(sample²) / N)` |
| `peak` | 峰值振幅 | `max(abs(sample))` |
| `mean` | DC 偏移 | `sum(sample) / N` |

**兼容固件：** 用户自定义 Arduino sketch（如 `sketch_jun16a`）输出中文文本：

```
检测到声音!
🔊 音量: 71.43%
```

Bridge 会自动解析并映射为 `level/peak/mean`（`source: "legacy"`）。

### 5.5 Arduino IDE 推荐配置

- Board: ESP32 Dev Module  
- Upload Speed: 921600  
- CPU: 240 MHz  
- Flash: 4MB  
- Port: COM3（以实际为准）

---

## 6. Node.js 串口桥接层

### 6.1 位置与依赖

```
bridge/
├── package.json
├── serial-bridge.js
└── node_modules/
```

依赖：`serialport`、`@serialport/parser-readline`、`ws`

### 6.2 职责

1. 打开 USB 串口，按行读取数据  
2. 解析 JSON / Legacy 文本格式  
3. 校验数值（NaN 置 0）  
4. 附加 `timestamp`，经 WebSocket 广播  
5. 串口断线自动重连（2.5s 间隔）  
6. 分级调试日志：`[serial]` / `[ws]`

### 6.3 启动命令

```bash
cd bridge
npm install
node serial-bridge.js --port COM3 --baud 115200 --ws 8765
```

| 参数 | 默认 | 说明 |
|------|------|------|
| `--port` | COM3 | 串口路径 |
| `--baud` | 115200 | 波特率 |
| `--ws` | 8765 | WebSocket 端口 |

环境变量：`SERIAL_PORT`、`SERIAL_BAUD`、`WS_PORT`

### 6.4 WebSocket 服务

- 地址：`ws://localhost:8765`
- 连接时发送：`{ "type": "bridge", "status": "connected", "timestamp": ... }`
- 音频指标广播格式见 [§10.2](#102-websocket-消息格式)

---

## 7. Web 前端应用

### 7.1 入口与运行

```
web-demo/
├── index.html      # 三视图 DOM 结构
├── style.css       # 全局样式
├── app.js          # 应用控制器 App
├── sketch.js       # p5.js 入口（setup/draw）
└── [模块].js       # 功能模块
```

启动：

```bash
cd web-demo
npx http-server -p 8000 -c-1
```

访问：http://localhost:8000

### 7.2 核心模块一览

| 模块文件 | 类名 | 职责 |
|----------|------|------|
| `app.js` | `App` | 视图路由、Library、样本 CRUD、全局状态 |
| `fieldRecorder.js` | `FieldRecorder` | 现场录音：开始/停止/命名/保存 |
| `esp32AudioAdapter.js` | `Esp32AudioAdapter` | WebSocket 客户端，实时指标平滑与视觉映射 |
| `audioAnalyzer.js` | `AudioAnalyzer` | 电脑麦克风实时分析（ESP32 在线时委托） |
| `sampleAnalyzer.js` | `SampleAnalyzer` | 离线文件 FFT、 pitch、时序分段 |
| `soundPersonalityAI.js` | `SoundPersonalityAI` | 规则型「声音人格」与身份文案 |
| `visualMapping.js` | `VisualMappingEngine` | 人格 → 视觉参数（palette、brush 等） |
| `soundTransformView.js` | `SoundTransformView` | Transform 六步透明管线 UI |
| `generativeField.js` | `GenerativeField` | 左侧 Sound Breathing 粒子场 |
| `brushGenerator.js` | `BrushGenerator` | 中央画布呼吸点刷 |
| `canvasInteraction.js` | `CanvasInteraction` | 画布布局、鼠标绑定、合成渲染 |
| `visualTransformation.js` | `VisualTransformation` | 顶部材料预览条 |
| `soundRecorder.js` | `SoundRecorder` | MediaRecorder 封装（备用） |
| `dataLogger.js` | `DataLogger` | 数据日志（扩展用） |

### 7.3 Library 录音模块（FieldRecorder）

**状态机：**

```
idle → active（recording）→ save（命名）→ idle
                              ↓ discard
                            idle
```

**行为：**

- `Start recording`：请求麦克风权限，启动 `MediaRecorder`（WebM/Opus）
- 录音中：计时器 + 电平条（ESP32 优先，否则 AnalyserNode）
- `Stop & save`：生成 Blob，进入命名面板
- 默认命名：`Field sound N`（ESP32 在线）或 `Nature sound N`
- 保存：调用 `App.addFile()`，标记 `source: "field-record"`

### 7.4 ESP32 前端适配器

`esp32AudioAdapter.js` 暴露：

```javascript
window.esp32AudioAdapter.audioData   // { level, peak, mean, timestamp }
esp32AudioAdapter.isConnected()      // 3 秒内有数据
esp32AudioAdapter.getNormalized()    // 0–1 归一化
esp32AudioAdapter.getVisualFeatures() // 视觉映射特征
esp32AudioAdapter.toAnalyzerFeatures() // 对接 AudioAnalyzer
```

**视觉映射规则：**

| ESP32 指标 | 视觉效果 |
|------------|----------|
| `level` | 粒子球半径、密度、volume |
| `peak` | 点描闪烁强度（alpha boost） |
| `mean` | 背景漂移、噪声时间偏移 |

UI 状态：左下角固定 pill `#esp32Status`（ESP32 live / offline / error）

### 7.5 样本数据结构

```javascript
{
  id: "s1700000000abc",       // 唯一 ID
  name: "bird",               // 显示名（用户命名或 AI 身份名）
  fileName: "bird.webm",      // 文件名
  file: File,                 // Blob 文件对象
  duration: 3.2,              // 秒
  waveformSnapshot: Float32Array(200),
  features: null | Object,    // SampleAnalyzer 输出
  aiResult: null | Object,    // SoundPersonalityAI 输出
  visualParams: null | Object,// VisualMappingEngine 输出
  status: "ready" | "analyzing" | "analyzed",
  source: "field-record" | "import"
}
```

---

## 8. 音频分析管线

### 8.1 Transform 六步（soundTransformView.js）

| Step | 名称 | 技术 | 输出 |
|------|------|------|------|
| 1 | Recognize | Web Audio 解码 + 滚动示波器 | 波形动画 |
| 2 | Amplitude | RMS / peak / dynamic range | 音量指标 |
| 3 | FFT | 纯 JS FFT（SampleAnalyzer） | 频谱图 |
| 4 | Pitch & Timbre | 自相关 pitch + 粗糙度/亮度 | 音色指标 |
| 5 | Map | VisualMappingEngine | 色板 + 映射参数 |
| 6 | Materialize | GenerativeField 预览 | Sound Breathing 粒子 |

### 8.2 SampleAnalyzer 特征集

```javascript
{
  volume, bass, mid, treble, brightness, energy, roughness,
  pitch, tempo, dynamicRange,
  spectrumProfile: Float32Array,
  sections: { beginning, middle, end }  // 时序三分法
}
```

算法要点：

- 单声道混音 → Hann 窗重叠 FFT  
- 自相关估计基频（pitch）  
- Onset 检测估计 tempo  
- 全段 + 三段时序分析供 AI 叙事使用  

### 8.3 SoundPersonalityAI

输入：归一化 audio features（不直接读麦克风）

输出：

```javascript
{
  personality: { energy, calmness, playfulness, curiosity, warmth },
  emotion: { ... },
  structure: { ... },
  identity: {
    name, interpretation, personalitySummary,
    structureSummary, temporalSummary, materialSummary
  }
}
```

### 8.4 Python Mode B + 可选 Qwen3-Omni 语义分析

本地链路（始终执行）：

`WAV → librosa 特征 → modifiers → brushParams → analysisExport（含 naturalArchetype / visualStructure）`

可选语义链路（仅 Python 服务端，不暴露 Key）：

1. 配置 `python-service/.env`（自 `.env.example` 复制）：
   - `SILICONFLOW_API_KEY`
   - `SILICONFLOW_OMNI_MODEL`（默认 `Qwen/Qwen3-Omni-30B-A3B-Instruct`）
   - `SILICONFLOW_BASE_URL`（默认 `https://api.siliconflow.cn/v1`）
   - `SILICONFLOW_OMNI_ENABLED`（默认 `1`）
2. `POST /analyze/wav` 将完整 WAV Base64 为 `audio_url`，调用硅基流动 `/chat/completions`
3. 响应增加 `semantic` 字段（开放识别，不预设五类）：

```json
{
  "soundLabel": "短标签",
  "description": "客观描述",
  "possibleSources": ["可能声源"],
  "audibleEvents": ["可听事件"],
  "confidence": 0.0
}
```

约束：

- **不**用 Qwen 结果改写 `naturalArchetype` / `strokePattern` / `brushParams`
- 无 Key、超时、HTTP 失败或 JSON 无效时：`semantic = null`，本地分析照常返回
- Key 只存在于服务端环境变量 / `.env`（已 gitignore），禁止写入前端或日志

实现文件：`python-service/siliconflow_omni.py`、`app.py`、`schemas.py`、`config.py`

---

## 9. 视觉生成系统

### 9.1 画布布局（canvasInteraction.js）

```
┌─────────────────────────────────────────────┐
│  Materials 条（Palette / Brush / Texture）   │
├──────────────┬──────────────────────────────┤
│ Sound        │                              │
│ Breathing    │     中央绘画画布              │
│ (28% 宽)     │     (72% 宽)                 │
│ GenerativeField                             │
└──────────────┴──────────────────────────────┘
```

**设计约束：** 不修改外层 canvas 布局、顶部 Color Palette/Brush/Texture 卡片位置。

### 9.2 Sound Breathing（generativeField.js）

- 12k–34k 粒子，离屏 `createGraphics` 层渲染  
- 透明背景 + `destination-out` 拖尾淡出  
- 音频驱动：volume → 球体半径；low → 涡流；high → 噪声频率  
- ESP32 在线时合并 `getVisualFeatures()`  
- 点描美学：`strokeWeight(1); point(x,y)`，绿色墨（palette index 1）

### 9.3 呼吸画笔（brushGenerator.js）

- 鼠标拖拽在中央画布 `addStroke()`  
- 每笔画 spawn 粒子簇（最多 72 簇 × ~320 粒子）  
- 与 GenerativeField 相同物理：向心/离心、Perlin 噪声流、Gaussian 重生  
- ESP32 实时数据同样影响闪烁与噪声漂移  

### 9.4 VisualMappingEngine

人格向量 → 视觉参数：

- `palette[]` — RGB 色板  
- `motionSpeed`、`particleDensity`、`shapeComplexity`  
- `colorVariation`、`directionChange`、`brushSize` 等  

---

## 10. 数据模型与接口协议

### 10.1 Serial 输出格式

**JSON（推荐）：**

```json
{"level":12345,"peak":67890,"mean":-1234}
```

**Legacy 文本（兼容）：**

```
检测到声音!
音量: 71.43%
```

**KV 备用：**

```
LEVEL:12345,PEAK:67890,MEAN:-1234
```

### 10.2 WebSocket 消息格式

```json
{
  "level": 12345,
  "peak": 67890,
  "mean": -1234,
  "timestamp": 1710000000000,
  "source": "json"
}
```

`source` 可选值：`json` | `legacy` | `kv`

### 10.3 全局运行时状态（window）

| 变量 | 设置时机 | 用途 |
|------|----------|------|
| `window.activeVisualParams` | Transform / Analysis | 当前视觉参数 |
| `window.activePersonality` | Transform / Analysis | 人格向量 |
| `window.activeAiResult` | Transform / Analysis | AI 身份与结构 |
| `window.activeAudioFeatures` | Transform / Analysis | 离线分析特征 |
| `window.esp32AudioAdapter` | App.init | ESP32 实时适配器 |
| `window.App` | app.js | 应用控制器 |

---

## 11. 项目文件结构

```
项目四sound ai coding/
├── 技术说明书.md              ← 本文档
├── README.md                  ← 快速启动（ESP32 管线）
│
├── esp32/
│   └── inmp441_bridge.ino     ← ESP32 推荐固件
│
├── bridge/
│   ├── package.json
│   ├── serial-bridge.js       ← Serial → WebSocket
│   └── node_modules/
│
└── web-demo/
    ├── index.html
    ├── style.css
    ├── app.js
    ├── sketch.js
    ├── fieldRecorder.js       ← 现场录音
    ├── esp32AudioAdapter.js   ← ESP32 WebSocket
    ├── audioAnalyzer.js
    ├── sampleAnalyzer.js
    ├── soundPersonalityAI.js
    ├── visualMapping.js
    ├── soundTransformView.js
    ├── generativeField.js
    ├── brushGenerator.js
    ├── canvasInteraction.js
    ├── visualTransformation.js
    ├── soundRecorder.js
    └── dataLogger.js
```

---

## 12. 部署与启动

### 12.1 完整启动顺序

```text
1. 烧录 ESP32 固件（esp32/inmp441_bridge.ino 或现有 sketch）
2. 关闭 Arduino Serial Monitor（释放 COM 口）
3. cd bridge && node serial-bridge.js --port COM3
4. cd web-demo && npx http-server -p 8000 -c-1
5. 浏览器打开 http://localhost:8000
6. 确认左下角「ESP32 live」
7. Start recording → 采集 → Transform → Draw
```

### 12.2 端口占用

| 服务 | 端口 |
|------|------|
| Web 前端 | 8000 |
| WebSocket Bridge | 8765 |
| ESP32 Serial | COM3（示例） |

### 12.3 常见问题

| 现象 | 处理 |
|------|------|
| `COM3: Access denied` | 关闭 Arduino Serial Monitor / 退出占用串口的程序 |
| `ESP32 offline` | 确认 Bridge 运行；检查 8765 端口 |
| 录音无权限 | 使用 localhost；浏览器允许麦克风 |
| Bridge 无 parsed 日志 | 检查固件输出格式；确认波特率 115200 |
| Transform 波形不动 | 确认 Web Audio 解码成功；刷新页面 |

---

## 13. 技术栈

| 层级 | 技术 |
|------|------|
| 硬件 | ESP32, INMP441, I2S |
| 固件 | Arduino ESP32 Core, driver/i2s.h |
| 桥接 | Node.js 18+, serialport 12, ws 8 |
| 前端框架 | 原生 HTML/CSS/JS |
| 可视化 | p5.js 1.9.0 + p5.sound |
| 音频分析 | Web Audio API + 自研 JS FFT |
| 实时通信 | WebSocket |
| 本地服务 | http-server |

---

## 14. 设计约束与原则

1. **布局稳定：** 不改动 Visual Generation Area 外层结构与顶部材料卡片位置  
2. **模块隔离：** Sound Breathing 使用离屏层；拖尾 fade 不使用纸色填充  
3. **Adapter 模式：** `audioAnalyzer.js` 保留；ESP32 通过 adapter 注入，不破坏 AI 层  
4. **采集优先：** Library 以录音为主流程，已移除文件上传入口  
5. **实时优先：** ESP32 通道为流式指标，非文件缓冲  
6. **可扩展：** 预留 PCM、FFT on-device、Bridge WAV 缓存等扩展点  

---

## 15. 已知限制

| 限制 | 说明 |
|------|------|
| 录音源 | 需烧录 PCM 固件 + 921600 Bridge；旧「音量%」固件**不能**保存音频 |
| ESP32 数据 | 录音时 PCM int16@16kHz；空闲时 JSON metrics |
| 浏览器 | 推荐 Chrome/Edge；WebM 解码依赖浏览器能力 |
| 串口独占 | Bridge 与 Arduino Monitor 不能同时打开 COM 口 |
| 离线 AI | SoundPersonalityAI 为规则引擎，非云端大模型 |
| 样本持久化 | 刷新页面后 Library 清空（未接后端存储） |

---

## 16. 未来扩展路线

### 16.1 硬件与固件

- [ ] ESP32 串流 PCM（512 samples/frame）→ Bridge 缓冲 WAV  
- [ ] 录音开始/停止命令：`{"cmd":"start"}` / `{"cmd":"stop"}`  
- [ ] 统一固件输出 JSON，弃用 Legacy 文本  

### 16.2 Bridge

- [ ] 录完自动写本地 `.wav` 供调试  
- [ ] 多客户端广播状态  
- [ ] 串口自动探测  

### 16.3 前端

- [ ] Library 样本 localStorage / IndexedDB 持久化  
- [ ] 录完可选「立即 Transform」  
- [ ] ESP32 PCM → Web Audio AudioWorklet 实时分析  
- [ ] 儿童向中文 UI 文案  

### 16.4 视觉与 AI

- [ ] 实时 FFT 叠加 ESP32 或 mic 流  
- [ ] 云端 LLM 生成声音身份叙事  
- [ ] 多样本混音材料  

---

## 17. 修改记录（Changelog）

> **维护规则：** 每次功能、界面、固件或协议变更后，在本节**顶部**追加一条记录（最新在上）。  
> 同步更新文首「版本号 / 更新日期」，并在对应章节修正正文。  
> 格式：`日期 · 版本 · 摘要 → 涉及文件`

---

### 2026-07-15 · v1.3 — SiliconFlow Qwen3-Omni 语义分析

| 项 | 内容 |
|----|------|
| **摘要** | `/analyze/wav` 可选返回开放识别字段 `semantic`；失败为 `null`，不改写本地 archetype / brush |
| **涉及文件** | `python-service/siliconflow_omni.py`、`app.py`、`schemas.py`、`config.py`、`.env.example` |
| **备注** | 见 §8.4；Key 仅服务端 `.env` |

---

### 2026-06-17 · v1.2 — ESP32 硬件 PCM 录音（取代电脑麦克风）

| 项 | 内容 |
|----|------|
| **摘要** | Library 录音改为 **ESP32 + INMP441 硬件 PCM**：Start → 串流 PCM → Stop → 前端拼 WAV 保存 |
| **固件** | `inmp441_bridge.ino`：`REC_START`/`REC_STOP` 控制；921600 波特率；二进制 PCM 帧 `0xA5 0x5A 0x01` |
| **Bridge** | 解析 PCM 二进制帧；WebSocket `{cmd:"record_start/stop"}`；转发 `{type:"pcm", data: base64}` |
| **前端** | `fieldRecorder.js` 仅硬件路径；`wavEncoder.js` 拼 16kHz WAV；样本 `source: "esp32-record"` |
| **涉及文件** | `esp32/inmp441_bridge.ino`、`bridge/serial-bridge.js`、`fieldRecorder.js`、`esp32AudioAdapter.js`、`wavEncoder.js` |
| **必须** | 重新烧录固件 + Bridge 使用 `--baud 921600`；**不可**再用仅输出「音量%」的旧 sketch 录音 |

---

### 2026-06-17 · v1.1 — 补充修改记录章节

| 项 | 内容 |
|----|------|
| **摘要** | 在技术说明书中新增 §17 修改记录，建立持续更新机制 |
| **涉及文件** | `技术说明书.md` |
| **备注** | 回溯记录 v1.0 开发周期内全部已知变更 |

---

### 2026-06-17 · v1.0 — 移除文件上传入口

| 项 | 内容 |
|----|------|
| **摘要** | 删除「Import audio file instead」按钮及隐藏上传区；Library 仅保留现场录音 |
| **涉及文件** | `web-demo/index.html`、`web-demo/app.js`（移除 `setupFileUpload` / `handleFiles`）、`web-demo/fieldRecorder.js` |
| **原因** | Demo 预设为儿童在自然中现场采集，不需要上传文件 |

---

### 2026-06-17 · v1.0 — 现场录音采集流程（Collect）

| 项 | 内容 |
|----|------|
| **摘要** | Library 主流程改为：Start recording → Stop → 命名 → Save；一次开始–结束 = 一个样本 |
| **涉及文件** | `web-demo/fieldRecorder.js`（新建）、`web-demo/index.html`、`web-demo/app.js`、`web-demo/style.css` |
| **行为** | 工作流改为 ① Collect → ② Transform → ③ Draw；样本标记 `source: "field-record"`；录音电平 ESP32 优先 |
| **备注** | 录音文件暂由浏览器 `MediaRecorder` 生成（WebM/Opus） |

---

### 2026-06-17 · v1.0 — Bridge 兼容 Legacy 固件文本

| 项 | 内容 |
|----|------|
| **摘要** | 串口桥接支持 `sketch_jun16a` 中文输出（`音量: XX%`、`检测到声音` 等），映射为 level/peak/mean |
| **涉及文件** | `bridge/serial-bridge.js` |
| **原因** | 用户实际烧录固件与 JSON 版 `inmp441_bridge.ino` 不一致 |

---

### 2026-06-17 · v1.0 — ESP32 实时音频管线

| 项 | 内容 |
|----|------|
| **摘要** | 完整链路：ESP32 → USB Serial → Node Bridge → WebSocket → 前端可视化 |
| **新建** | `esp32/inmp441_bridge.ino`、`bridge/serial-bridge.js`、`bridge/package.json`、`web-demo/esp32AudioAdapter.js`、`README.md` |
| **修改** | `web-demo/app.js`（初始化 adapter）、`generativeField.js`、`brushGenerator.js`、`audioAnalyzer.js`、`index.html`、`style.css`（`#esp32Status`） |
| **映射** | level → 粒子密度/体积；peak → 闪烁；mean → 漂移/噪声偏移 |
| **协议** | WebSocket `ws://localhost:8765`；JSON `{ level, peak, mean, timestamp }` |

---

### 2026-06-17 · v1.0 — Transform 波形改为滚动示波器

| 项 | 内容 |
|----|------|
| **摘要** | Step1 波形由静态「已播/未播 + 播放头」改为 Web Audio 解码采样驱动的水平滚动示波器 |
| **涉及文件** | `web-demo/soundTransformView.js` |
| **后续微调** | 去掉人为上下起伏（`floatY`）避免头晕；去掉拖尾叠影，每帧全清 + 单线绘制 |

---

### 2026-06-17 · v1.0 — Sound Transform 透明管线页

| 项 | 内容 |
|----|------|
| **摘要** | 新增 Transform 视图：Recognize → 音量 → FFT → 音色 → 映射 → Sound Breathing 预览 |
| **涉及文件** | `web-demo/soundTransformView.js`（新建）、`web-demo/index.html`（`#transformView`）、`web-demo/style.css`、`web-demo/app.js`（Library → Transform → Analysis 路由） |
| **备注** | 分析使用 Web Audio + `SampleAnalyzer`（非 p5 FFT） |

---

### 2026-06-17 · v1.0 — 呼吸画笔（Brush = Sound Breathing）

| 项 | 内容 |
|----|------|
| **摘要** | 中央画布笔触改为与 GenerativeField 同源的粒子簇动画；拖拽即 spawn 活体材料 |
| **涉及文件** | `web-demo/brushGenerator.js`、`web-demo/canvasInteraction.js`、`web-demo/visualTransformation.js` |

---

### 2026-06-17 · v1.0 — Sound Breathing 粒子场优化

| 项 | 内容 |
|----|------|
| **摘要** | 左侧 sound breathing：离屏层渲染、透明背景、`destination-out` 拖尾、Perlin 噪声流、音量/FFT 映射、点描美学 |
| **涉及文件** | `web-demo/generativeField.js` |
| **修复** | 去除纸色半透明填充（解决红黄/奶油色光晕）；提高粒子密度与拖尾参数 |
| **约束** | 不改外层 canvas 布局与 DOM 位置 |

---

### 版本对照表

| 版本 | 日期 | 里程碑 |
|------|------|--------|
| v1.3 | 2026-07-15 | SiliconFlow Qwen3-Omni 语义分析（`semantic`） |
| v1.2 | 2026-06-17 | ESP32 硬件 PCM 录音 |
| v1.1 | 2026-06-17 | 技术说明书 + 修改记录机制 |
| v1.0 | 2026-06-17 | ESP32 管线、现场录音、Transform 页、Sound Breathing/Brush 体系 |

---

## 附录 A：脚本加载顺序（index.html）

```html
p5.js → p5.sound → soundPersonalityAI → visualMapping → visualTransformation
→ generativeField → sampleAnalyzer → soundTransformView → brushGenerator
→ canvasInteraction → fieldRecorder → audioAnalyzer → esp32AudioAdapter
→ app.js → sketch.js
```

## 附录 B：关键类关系

```
App
 ├── FieldRecorder        (Library 录音)
 ├── SoundTransformView   (Transform 管线)
 ├── esp32AudioAdapter    (ESP32 实时)
 └── canvasInteraction    (p5 画布)
      ├── GenerativeField (左)
      ├── BrushGenerator  (中)
      └── VisualTransformation (顶)

SampleAnalyzer → SoundPersonalityAI → VisualMappingEngine
                      ↓
              generativeField / brushGenerator
```

---

**文档维护：** 功能变更时请同步更新本文档对应章节，并在 [§17 修改记录](#17-修改记录changelog) **顶部**追加一条。  
**快速上手：** 见项目根目录 `README.md`。
