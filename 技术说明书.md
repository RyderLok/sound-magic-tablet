# Piko — 产品技术说明书

**版本：** 1.6  
**更新日期：** 2026-08-07  
**仓库：** https://github.com/RyderLok/sound-magic-tablet  
**协议细节：** [`交互基准.md`](./交互基准.md) · **iPad 总览：** [`Piko中文交互说明与iPad就绪.md`](./Piko中文交互说明与iPad就绪.md)

---

## 目录

1. [产品概述](#1-产品概述)
2. [目标用户与使用场景](#2-目标用户与使用场景)
3. [产品工作流](#3-产品工作流)
4. [系统架构](#4-系统架构)
5. [硬件与固件层](#5-硬件与固件层)
6. [Node.js 串口桥接层](#6-nodejs-串口桥接层)
7. [Web 前端应用](#7-web-前端应用)
8. [音频分析管线](#8-音频分析管线)
9. [视觉生成系统](#9-视觉生成系统)
10. [数据模型与接口协议](#10-数据模型与接口协议)
11. [项目文件结构](#11-项目文件结构)
12. [部署与启动](#12-部署与启动)
13. [技术栈](#13-技术栈)
14. [设计约束与原则](#14-设计约束与原则)
15. [已知限制](#15-已知限制)
16. [未来扩展路线](#16-未来扩展路线)
17. [修改记录（Changelog）](#17-修改记录changelog)

---

## 1. 产品概述

**Piko** 是一套面向自然情境的声音采集与生成式视觉系统。用户（预设 demo：儿童在自然环境中）发现一种声音 → 录制 → 停止 → 保存为独立样本 → 命名 → 系统将声音「理解」并转化为可绘制的**活体视觉材料（living material）**，最终在画布上用该材料进行创作。

产品核心理念：

> **声音不是被播放的对象，而是被转化的材料。**  
> Collect → Transform → Draw

系统运行时分两条拓扑（**产品终态 = iPad 独立**；桌面 USB 为开发脚手架）：

| 层级 | 生产（iPad） | 开发（Mac/Windows） |
|------|--------------|---------------------|
| 采集硬件 | ESP32 + INMP441，Wi‑Fi `POST /sounds/upload_pcm` | 同固件；或 USB 串口 PCM |
| 接收 / 分析 | `ios/PikoShell` · `LocalPikoGateway` **:8001** + 可选 Qwen / Supabase anon | Python FastAPI **:8001** + 可选 USB Bridge **:8765** |
| 交互前端 | App 内嵌 `web-demo`（构建时 `sync-web-demo.sh`） | 浏览器 `localhost:8000` |
| 云端曲库 | Supabase Storage + `sounds` 表（anon） | 同契约；Python 可用 service_role |

---

## 2. 目标用户与使用场景

### 2.1 预设 Demo 场景（产品面）

儿童在自然环境中行走，每发现一种有趣声音：

1. ESP 按键录音 → Wi‑Fi 上传到 **iPad 本机网关**（或开发机 Python）
2. **Transfer** 页展示上传进度 → 进入 **New Sounds**（仅本会话新声）
3. 选中最多 **5** 段 → **Transform** 生成 Brush → **Draw** 色盘画板共画
4. 保存画作到 **My Gallery**；之后可再点开同一张**继续绘画**（保存覆盖原作）
5. 历史声音在 **My sounds**；画作多选可删除 / 打印流程入口

### 2.2 与旧版差异

| 旧流程 | 当前流程 |
|--------|----------|
| 仅桌面浏览器 + USB Bridge | **生产：iPad App 独立**；桌面为开发路径 |
| Library 单页录音为主 | **Collect 轮播** → Transfer / Draw / Gallery |
| 上传即混入历史列表 | **New Sounds** = 本 Transfer 会话；**My sounds** = 历史 |
| 画作只展示 | **My Gallery 点击续画**，再保存更新同一 `id` |
| 单样本画室 | **Plate ≤5** 多材料共画 |

---

## 3. 产品工作流

```
┌─────────────────────────────────────────────────────────────────┐
│  Collect（轮播入口）                                             │
│  中心卡：Input → Transfer │ Draw → 画板 │ Gallery → My Gallery │
└───────────────┬─────────────────────┬───────────────────────────┘
                │                     │
                ▼                     ▼
┌──────────────────────────┐   ┌──────────────────────────────────┐
│ Transfer → New Sounds    │   │ My Gallery                       │
│ ESP Wi‑Fi 上传 / 会话入库 │   │ 点画作 → 画板续画 → 保存覆盖     │
│ → 勾选 ≤5 → Transform    │   │ （多选：删除 / 打印入口）         │
└──────────────┬───────────┘   └──────────────────────────────────┘
               ▼
┌─────────────────────────────────────────────────────────────────┐
│ Transform → Draw（analysisView + PikoCanvas 顶栏）               │
│ Plate ≤5 brush 共画；Save → GalleryStore（IndexedDB）            │
└─────────────────────────────────────────────────────────────────┘
```

### 3.1 视图 / 屏幕切换

| 屏幕 / 视图 | 入口 | 说明 |
|-------------|------|------|
| Collect | Splash 后默认 | 三磁贴轮播（`pikoCollectCarousel`） |
| Transfer | Collect → Input | 录音笔上传等待 / 进度 |
| New Sounds (`sounds`) | Transfer 完成 | **本会话**新声；勾选 ≤5 进画板 |
| My sounds | 曲库历史入口 | 历史声音；与 New Sounds 分离 |
| Transform / analysis | 样本 Transform / Plate | 分析管线 + 色盘画板 |
| My Gallery (`gallery`) | Collect → Gallery | 已存画作；点击续画 |
| `#acousticAnalysisModal` | 样本「分析」 | 声学弹窗 + 应用 Brush |

前端路由：`web-demo/pikoRouter.js`；业务控制器：`web-demo/app.js`（`App`）；色盘：`plateManager.js`。

### 3.2 My Gallery 续画

1. `PikoGalleryScreen`：非多选时点击卡片 → `App.resumeGalleryArtwork(id)`  
2. `GalleryStore.get` 取 IndexedDB PNG → `CanvasInteraction.loadArtworkFromBlob` 铺到 `persistentLayer`  
3. 若已有 Plate brush 则套用；否则用默认视觉参数仍可画  
4. 保存时若 `App.editingArtworkId` 有值 → **覆盖同一画作 id**（保留 `createdAt`，更新 `updatedAt`）

### 3.3 五类材料 → 笔触（Qwen 定算法 · 本地调类内差异）

| category（Qwen） | 中文 | strokePattern（笔刷算法） |
|------------------|------|---------------------------|
| `birds` | 鸟类 | `scatter_points` |
| `wind_leaves` | 风与树叶 | `flow_field` |
| `water` | 水 | `wave_ripple` |
| `material_impact` | 材质撞击 | `impact_burst` |
| `insects_amphibians` | 昆虫两栖 | `pulse_grid` |

- **Qwen**：唯一「声音类别」来源 → 选定上表算法  
- **本地声学**：`acousticFeatures` → 在该类 `BRUSH_RANGES` 内生成 `brushParams`（同类录音可明显不同）  
- **不再**用本地 `classify_natural_archetype` 决定类别/造型  
- 详表：[`python-service/BRUSH_ACOUSTIC_MAPPING.md`](./python-service/BRUSH_ACOUSTIC_MAPPING.md)

---

## 4. 系统架构

### 4.1 生产拓扑（iPad 独立 · 北星）

```
        手机热点
     ┌──────┴──────┐
     ▼             ▼
  ESP32         iPad · PikoShell
  INMP441        ├─ WKWebView 内嵌 web-demo
  Wi‑Fi STA      ├─ LocalPikoGateway :8001
     │           │    /health · /sounds/upload_pcm · /sounds
     │           │    /analyze/wav（可选 Qwen Key）
     └─ PCM ────▶└─ Documents 本地曲库 + Supabase anon（可选）
```

ESP 固件 `WIFI_UPLOAD_HOST` = iPad 热点 IP（App Settings 可见）。密钥勿进仓库：`Secrets.plist`、`esp32/wifi_secrets.h`（仅 `*.example` 入库）。

### 4.2 开发拓扑（桌面）

```
  浏览器 :8000 (web-demo)
       │
       ├─► Python FastAPI :8001  (/sounds/* · /analyze/wav · Supabase)
       │
       └─► (可选) Bridge :8765 ◄── USB Serial ◄── ESP32
```

Windows：`start-windows.ps1`；Mac 临时：`./start-mac.sh`（`.venv-mac`，不碰 Windows `.venv`）。

### 4.3 音频通道

| 通道 | 生产 | 开发 | 用途 |
|------|------|------|------|
| **Wi‑Fi 上传** | ESP → iPad `:8001` `upload_pcm` | ESP → Mac/PC Python 同路径 | 主录音入库（`source=esp32-wifi`） |
| **USB PCM** | — | Serial → Bridge → WS | 桌面联调 / 实时电平 |
| **分析** | iPad Qwen 客户端或网关转发 | Python + SiliconFlow | 五类语义 → `strokePattern` |

USB 录音旧路径仍可用：`record_start` → PCM 帧 → 前端拼 WAV。Bridge 波特率以运行手册为准，当前默认 **500000**。

---

## 5. 硬件与固件层

### 5.1 硬件清单

| 组件 | 型号/规格 |
|------|-----------|
| 主控 | ESP32 Dev Module (ESP32-D0WD-V3) |
| 麦克风 | INMP441（I2S 数字麦克风） |
| 连接 | USB 转串口（如 Silicon Labs CP210x → COM3） |

### 5.2 INMP441 接线

| INMP441 引脚 | ESP32 |
|--------------|-------|
| SCK (BCLK) | GPIO 14 |
| WS (LRCLK) | GPIO 15 |
| SD | GPIO 32 |
| VDD | 3.3V |
| GND | GND |
| L/R | GND（左声道） |

### 5.3 音频参数

| 参数 | 值 |
|------|-----|
| 采样率 | 16000 Hz |
| I2S 位宽 | 32-bit 输入 |
| 有效位深 | 24-bit（`raw >> 8`） |
| 每帧样本数 | 512 |
| 串口波特率 | 默认 **500000**（USB Bridge；以固件与启动参数为准） |
| Wi‑Fi 上传 | `POST http://{WIFI_UPLOAD_HOST}:8001/sounds/upload_pcm` |

### 5.4 固件文件

**推荐固件：** `esp32/inmp441_bridge.ino`（含 Wi‑Fi 上传与 USB 双路径；密钥见 `wifi_secrets.h.example`）

USB 空闲时每帧可输出 JSON metrics：

```json
{"level":12345,"peak":67890,"mean":-1234}
```

| 字段 | 含义 | 计算方式 |
|------|------|----------|
| `level` | RMS 能量 | `sqrt(sum(sample²) / N)` |
| `peak` | 峰值振幅 | `max(abs(sample))` |
| `mean` | DC 偏移 | `sum(sample) / N` |

**兼容固件：** 用户自定义 Arduino sketch（如 `sketch_jun16a`）输出中文文本：

```
检测到声音!
🔊 音量: 71.43%
```

Bridge 会自动解析并映射为 `level/peak/mean`（`source: "legacy"`）。

### 5.5 Arduino IDE 推荐配置

- Board: ESP32 Dev Module  
- Upload Speed: 921600  
- CPU: 240 MHz  
- Flash: 4MB  
- Port: COM3（以实际为准）

---

## 6. Node.js 串口桥接层

### 6.1 位置与依赖

```
bridge/
├── package.json
├── serial-bridge.js
└── node_modules/
```

依赖：`serialport`、`@serialport/parser-readline`、`ws`

### 6.2 职责

1. 打开 USB 串口，按行读取数据  
2. 解析 JSON / Legacy 文本格式  
3. 校验数值（NaN 置 0）  
4. 附加 `timestamp`，经 WebSocket 广播  
5. 串口断线自动重连（2.5s 间隔）  
6. 分级调试日志：`[serial]` / `[ws]`

### 6.3 启动命令

```bash
cd bridge
npm install
node serial-bridge.js --port COM3 --baud 115200 --ws 8765
```

| 参数 | 默认 | 说明 |
|------|------|------|
| `--port` | COM3 | 串口路径 |
| `--baud` | 115200 | 波特率 |
| `--ws` | 8765 | WebSocket 端口 |

环境变量：`SERIAL_PORT`、`SERIAL_BAUD`、`WS_PORT`

### 6.4 WebSocket 服务

- 地址：`ws://localhost:8765`
- 连接时发送：`{ "type": "bridge", "status": "connected", "timestamp": ... }`
- 音频指标广播格式见 [§10.2](#102-websocket-消息格式)

---

## 7. Web 前端应用

### 7.1 入口与运行

```
web-demo/
├── index.html      # 三视图 DOM 结构
├── style.css       # 全局样式
├── app.js          # 应用控制器 App
├── sketch.js       # p5.js 入口（setup/draw）
└── [模块].js       # 功能模块
```

启动：

```bash
cd web-demo
npx http-server -p 8000 -c-1
```

访问：http://localhost:8000

### 7.2 核心模块一览

| 模块文件 | 类名 / 对象 | 职责 |
|----------|-------------|------|
| `app.js` | `App` | Library/Plate、`enterPlateStudio`、`resumeGalleryArtwork`、全局状态 |
| `pikoRouter.js` | `PikoRouter` | Collect / Transfer / Sounds / Gallery / analysis 切屏 |
| `pikoSoundsScreen.js` | — | New Sounds（本 Transfer 会话批次） |
| `pikoMySoundsScreen.js` | — | My sounds 历史 |
| `pikoGalleryScreen.js` | — | My Gallery；点击续画 / 多选 |
| `pikoCanvasScreen.js` | — | 画板顶栏、保存到 Gallery |
| `galleryStore.js` | `GalleryStore` | IndexedDB `PikoGallery` / `artworks`（PNG Blob） |
| `soundsApiClient.js` | — | `/sounds` 与会话批次（不回填历史污染 New Sounds） |
| `soundColorEngine.js` | — | 声音卡片色预览 |
| `fieldRecorder.js` | `FieldRecorder` | 现场录音：ESP32 USB PCM → WAV → 命名/保存 |
| `esp32AudioAdapter.js` | `Esp32AudioAdapter` | WebSocket 客户端（桌面 Bridge） |
| `sampleLibraryStore.js` | — | IndexedDB 样本持久化（含 audioBlob） |
| `plateManager.js` | — | 色盘 ≤5，Plate Studio 切换 |
| `audioAnalyzer.js` | `AudioAnalyzer` | 实时分析辅助（ESP32 在线时委托） |
| `sampleAnalyzer.js` | `SampleAnalyzer` | 离线文件 FFT、pitch、时序分段 |
| `soundPersonalityAI.js` | `SoundPersonalityAI` | 规则型「声音人格」与身份文案 |
| `visualMapping.js` | `VisualMappingEngine` | 人格 → 视觉参数（palette、brush 等） |
| `soundTransformView.js` | `SoundTransformView` | Transform 六步透明管线 UI |
| `acousticAnalysisWindow.js` | — | 声学分析弹窗 + Qwen 语义展示 |
| `generativeField.js` | `GenerativeField` | 左侧 Sound Breathing 粒子场 |
| `brushGenerator.js` | `BrushGenerator` | 中央画布五类笔触 |
| `canvasInteraction.js` | `CanvasInteraction` | 画布、导出 PNG、`loadArtworkFromBlob` |
| `visualTransformation.js` | `VisualTransformation` | 顶部材料预览条 |
| `pythonEnhancementClient.js` | — | Python REST/WS 客户端 |

### 7.3 Library 录音模块（FieldRecorder）

**状态机：**

```
idle → active（recording）→ save（命名）→ idle
                              ↓ discard
                            idle
```

**行为：**

- `Start recording`：向 Bridge 发 `record_start`，缓冲 ESP32 PCM（不用电脑麦克风）
- 录音中：计时器 + 电平条（ESP32 metrics）；可选 Keyes 同步启停
- `Stop`：`record_stop` → `encodeWavFromPcmBytes()` → 试听 + 命名面板
- 默认命名：`Field sound N` 等
- 保存：`App.addFile()`，标记 `source: "esp32-record"`，写入 IndexedDB

### 7.4 ESP32 前端适配器

`esp32AudioAdapter.js` 暴露：

```javascript
window.esp32AudioAdapter.audioData   // { level, peak, mean, timestamp }
esp32AudioAdapter.isConnected()      // 3 秒内有数据
esp32AudioAdapter.getNormalized()    // 0–1 归一化
esp32AudioAdapter.getVisualFeatures() // 视觉映射特征
esp32AudioAdapter.toAnalyzerFeatures() // 对接 AudioAnalyzer
```

**视觉映射规则：**

| ESP32 指标 | 视觉效果 |
|------------|----------|
| `level` | 粒子球半径、密度、volume |
| `peak` | 点描闪烁强度（alpha boost） |
| `mean` | 背景漂移、噪声时间偏移 |

UI 状态：左下角固定 pill `#esp32Status`（ESP32 live / offline / error）

### 7.5 样本数据结构

```javascript
{
  id: "s1700000000abc",
  name: "bird",
  fileName: "bird.wav",
  file: File,                 // 16kHz mono WAV Blob
  duration: 3.2,
  waveformSnapshot: Float32Array(200),
  features: null | Object,
  aiResult: null | Object,
  visualParams: null | Object,
  status: "ready" | "analyzing" | "analyzed",
  source: "esp32-record" | "field-record" | "import"
}
```

持久化：`sampleLibraryStore.js` → IndexedDB `SoundMagicTablet` / `samples`（含 `audioBlob`）。色盘选中 id：`localStorage` `smt_plate_brush_ids`（最多 5）。

画作持久化：`galleryStore.js` → IndexedDB `PikoGallery` / `artworks`（`id`、`title`、`width`、`height`、`createdAt`、`updatedAt`、`imageBlob`）。

---

## 8. 音频分析管线

### 8.1 Transform 六步（soundTransformView.js）

| Step | 名称 | 技术 | 输出 |
|------|------|------|------|
| 1 | Recognize | Web Audio 解码 + 滚动示波器 | 波形动画 |
| 2 | Amplitude | RMS / peak / dynamic range | 音量指标 |
| 3 | FFT | 纯 JS FFT（SampleAnalyzer） | 频谱图 |
| 4 | Pitch & Timbre | 自相关 pitch + 粗糙度/亮度 | 音色指标 |
| 5 | Map | VisualMappingEngine | 色板 + 映射参数 |
| 6 | Materialize | GenerativeField 预览 | Sound Breathing 粒子 |

### 8.2 SampleAnalyzer 特征集

```javascript
{
  volume, bass, mid, treble, brightness, energy, roughness,
  pitch, tempo, dynamicRange,
  spectrumProfile: Float32Array,
  sections: { beginning, middle, end }  // 时序三分法
}
```

算法要点：

- 单声道混音 → Hann 窗重叠 FFT  
- 自相关估计基频（pitch）  
- Onset 检测估计 tempo  
- 全段 + 三段时序分析供 AI 叙事使用  

### 8.3 SoundPersonalityAI

输入：归一化 audio features（不直接读麦克风）

输出：

```javascript
{
  personality: { energy, calmness, playfulness, curiosity, warmth },
  emotion: { ... },
  structure: { ... },
  identity: {
    name, interpretation, personalitySummary,
    structureSummary, temporalSummary, materialSummary
  }
}
```

### 8.4 Python Mode B：Qwen 定类 + 本地类内笔刷

**当前管线：**

```
WAV
 → Qwen（五类）→ category → strokePattern / visualStructure
 → librosa 声学特征 acousticFeatures（不定类）
 → brush_mapper（BRUSH_RANGES 内）→ brushParams
 → analysisExport.unifiedBrush
 → 前端五套 BrushGenerator
```

| 对比项 | 之前（v1.3–1.4 文档） | 现在（代码 + 本节省） |
|--------|----------------------|----------------------|
| 谁定类别 | 本地 `classify_natural_archetype`（规则打分） | **仅 Qwen** |
| 谁定笔刷算法 | 本地 → `strokePattern` | **Qwen → strokePattern** |
| 谁定疏密/大小等 | 混在特征→modifiers，音量易连坐 | **本地声学分轴映射**（音量≠密度≠速度） |
| Qwen 失败 | 仍可有本地类别笔刷 | 无 `strokePattern`；提示重试 |
| 同类异录 | 易被模板/音量抹平 | 同算法下 particleSize/trail/gap 等可分 |

声学轴（摘要）：音量→尺度；频率→细节；粗糙→湍流；节奏→spawn/pulse；连续→trail/gap；震动=0.45·rough+0.30·flux+0.15·rms+0.10·onset。

配置与调用：

1. `python-service/.env`：`SILICONFLOW_API_KEY` 等  
2. `POST /analyze/wav` → `semantic` + `analysisExport.category` / `strokePattern` / `acousticFeatures` / `brushParams`  
3. 失败：`semanticError`（「声音识别失败，请重试」）  
4. Key 仅服务端，不进前端  

实现：`siliconflow_omni.py`、`acoustic_features.py`、`brush_mapper.py`、`audio_analyzer.py`、`app.py`；前端 `brushGenerator.js`、`naturalSoundArchetypes.js`。

---

## 9. 视觉生成系统

### 9.1 画布布局（canvasInteraction.js）

```
┌─────────────────────────────────────────────┐
│  Materials 条（Palette / Brush / Texture）   │
├──────────────┬──────────────────────────────┤
│ Sound        │                              │
│ Breathing    │     中央绘画画布              │
│ (28% 宽)     │     (72% 宽)                 │
│ GenerativeField                             │
└──────────────┴──────────────────────────────┘
```

**设计约束：** 不修改外层 canvas 布局、顶部 Color Palette/Brush/Texture 卡片位置。

### 9.2 Sound Breathing（generativeField.js）

- 12k–34k 粒子，离屏 `createGraphics` 层渲染  
- 透明背景 + `destination-out` 拖尾淡出  
- 音频驱动：volume → 球体半径；low → 涡流；high → 噪声频率  
- ESP32 在线时合并 `getVisualFeatures()`  
- 点描美学：`strokeWeight(1); point(x,y)`，绿色墨（palette index 1）

### 9.3 呼吸画笔（brushGenerator.js）

- 鼠标拖拽在中央画布 `addStroke()`  
- 每笔画 spawn 粒子簇（最多 72 簇 × ~320 粒子）  
- 与 GenerativeField 相同物理：向心/离心、Perlin 噪声流、Gaussian 重生  
- ESP32 实时数据同样影响闪烁与噪声漂移  

### 9.4 VisualMappingEngine

人格向量 → 视觉参数：

- `palette[]` — RGB 色板  
- `motionSpeed`、`particleDensity`、`shapeComplexity`  
- `colorVariation`、`directionChange`、`brushSize` 等  

---

## 10. 数据模型与接口协议

### 10.1 Serial 输出格式

**JSON（推荐）：**

```json
{"level":12345,"peak":67890,"mean":-1234}
```

**Legacy 文本（兼容）：**

```
检测到声音!
音量: 71.43%
```

**KV 备用：**

```
LEVEL:12345,PEAK:67890,MEAN:-1234
```

### 10.2 WebSocket 消息格式

```json
{
  "level": 12345,
  "peak": 67890,
  "mean": -1234,
  "timestamp": 1710000000000,
  "source": "json"
}
```

`source` 可选值：`json` | `legacy` | `kv`

### 10.3 HTTP 声音契约（生产 / 开发共用）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/health` | 健康检查；含 omni / supabase 配置态 |
| POST | `/sounds/upload_pcm` | 原始 PCM16 LE → WAV 入库；`source=esp32-wifi` |
| GET | `/sounds` | 曲库列表 |
| POST | `/analyze/wav` | 声学 + 可选 Qwen 语义 → brush |

细则见 [`交互基准.md`](./交互基准.md)。

### 10.4 全局运行时状态（window）

| 变量 | 设置时机 | 用途 |
|------|----------|------|
| `window.activeVisualParams` | Transform / Analysis / Gallery 续画 | 当前视觉参数 |
| `window.activePersonality` | Transform / Analysis | 人格向量 |
| `window.activeAiResult` | Transform / Analysis | AI 身份与结构 |
| `window.activeAudioFeatures` | Transform / Analysis | 离线分析特征 |
| `window.activeBrushParams` | Plate / Transform | 当前笔刷参数 |
| `window.esp32AudioAdapter` | App.init | ESP32 实时适配器（桌面） |
| `window.App` | app.js | 应用控制器（含 `editingArtworkId`） |
| `window.GalleryStore` | galleryStore.js | 画作 IndexedDB |
| `window.PikoRouter` | pikoRouter.js | 产品面切屏 |

---

## 11. 项目文件结构

```
sound-magic-tablet/
├── 技术说明书.md / README.md / AGENTS.md / 交互基准.md
├── Piko中文交互说明与iPad就绪.md
├── start-windows.ps1 / start-mac.sh
│
├── esp32/
│   ├── inmp441_bridge.ino          ← USB + Wi‑Fi 上传固件
│   └── wifi_secrets.h.example      ← 密钥模板（真实 wifi_secrets.h 不入库）
│
├── bridge/
│   └── serial-bridge.js            ← USB Serial → WebSocket :8765
│
├── python-service/                 ← 开发机分析 / 上传接收
│   ├── app.py · config.py · supabase_sounds.sql
│   └── start-python.ps1 / start-python-mac.sh
│
├── ios/PikoShell/                  ← 生产：iPad 独立运行时
│   ├── PikoShell/*.swift           ← LocalPikoGateway · WebContainer · Settings
│   ├── Secrets.plist.example
│   ├── sync-web-demo.sh            ← 构建时打包 web-demo → App WebDemo/
│   └── README.md · XCODE_HANDOFF.md
│
└── web-demo/                       ← 产品 UI（浏览器与 iPad 共用）
    ├── index.html · style.css · app.js · sketch.js
    ├── pikoRouter.js · piko*Screen.js · galleryStore.js
    ├── plateManager.js · brushGenerator.js · canvasInteraction.js
    └── soundsApiClient.js · soundColorEngine.js · …
```

---

## 12. 部署与启动

### 12.1 生产（iPad）

1. Xcode 打开 `ios/PikoShell`，真机 Run（构建会同步最新 `web-demo`）  
2. Settings：开本机网关；配置 Supabase **anon** / SiliconFlow Key（可选）  
3. 复制「ESP 上传地址」→ 写入 ESP `WIFI_UPLOAD_HOST` 并烧录  
4. 仅热点 + ESP + iPad（可不跑 Mac Python）  
5. Collect → Input 录音入库 → New Sounds → Transform → Draw → Gallery  

### 12.2 开发（Windows 为主）

```text
1. 烧录 ESP32 固件；关闭 Arduino Serial Monitor
2. start-windows.ps1   （或分别起 Bridge / Python / web-demo）
3. Bridge：COM3 @ 500000 → :8765
4. Python：start-python.ps1 → :8001（health 含 omni）
5. 前端：http://localhost:8000
```

Mac 临时：`./start-mac.sh`（`.venv-mac`）。

### 12.3 端口占用

| 服务 | 端口 |
|------|------|
| Web 前端（开发） | 8000 |
| 分析 / 上传网关 | **8001**（Python 或 iPad LocalPikoGateway） |
| USB WebSocket Bridge | 8765 |
| ESP 控制面（固件） | 8080（见交互基准） |
| ESP Serial | COM3 / `/dev/cu.*`（示例） |

### 12.4 GitHub 部署

「部署到 GitHub」= 源码 `commit` + `push origin main` → https://github.com/RyderLok/sound-magic-tablet  

**不入库：** `.env`、`Secrets.plist`、`esp32/**/wifi_secrets.h`、`python-service/.venv*`、`web-demo/_shots/`。

### 12.5 常见问题

| 现象 | 处理 |
|------|------|
| `COM3: Access denied` | 关闭 Arduino Serial Monitor |
| `ESP32 offline`（桌面） | 确认 Bridge :8765；波特率 500000 |
| iPad 无法打开 App | 设置 → VPN 与设备管理 → 信任开发者 |
| Gallery 点画无反应 | 需含 `resumeGalleryArtwork` 的 web-demo；重装 App 同步 WebDemo |
| New Sounds 出现旧声 | 确认 `soundsApiClient` 会话批次逻辑已部署 |
| Transform 波形不动 | Web Audio 解码；刷新页面 |

---

## 13. 技术栈

| 层级 | 技术 |
|------|------|
| 硬件 | ESP32, INMP441, I2S, Wi‑Fi STA |
| 固件 | Arduino ESP32 Core |
| 桥接（开发） | Node.js, serialport, ws |
| 分析（开发） | Python FastAPI, librosa, SiliconFlow Qwen |
| iPad 运行时 | SwiftUI, WKWebView, LocalPikoGateway |
| 云端 | Supabase Storage + Postgres |
| 前端 | 原生 HTML/CSS/JS, p5.js, Web Audio, IndexedDB |
| 实时通信（开发） | WebSocket :8765 |

---

## 14. 设计约束与原则

1. **布局稳定：** 不改动 Visual Generation Area 外层结构与顶部材料卡片位置  
2. **模块隔离：** Sound Breathing 使用离屏层；拖尾 fade 不使用纸色填充  
3. **Adapter 模式：** `audioAnalyzer.js` 保留；ESP32 通过 adapter 注入，不破坏 AI 层  
4. **采集优先：** 生产以 ESP Wi‑Fi 上传为主；USB Bridge 为开发脚手架  
5. **双轨分析：** Qwen 定笔刷算法；本地声学只调类内参数（见 §8.4）  
6. **Plate ≤5：** 多材料共画，换 brush 不清空笔迹  
7. **会话隔离：** New Sounds ≠ My sounds；Gallery 续画覆盖同一 `id`  
8. **密钥边界：** 禁止把 service_role / API Key 写进前端或对话日志；iPad 仅 anon  
9. **跨平台：** Windows 为主开发环境；Mac 改动不覆盖 `.venv` / 不批量改换行  

---

## 15. 已知限制

| 限制 | 说明 |
|------|------|
| 录音源 | 生产需 Wi‑Fi 固件 + 接收端 :8001；USB 需 Bridge |
| ESP32 数据 | Wi‑Fi：`upload_pcm`；USB：PCM int16@16kHz + JSON metrics |
| 浏览器 / WebView | 桌面推荐 Chrome/Edge；iPad 为 WKWebView |
| 串口独占 | Bridge 与 Arduino Monitor 不能同时开 COM |
| 离线人格 | SoundPersonalityAI 为规则引擎；云端语义走 Qwen（需 Key） |
| 样本 / 画作持久化 | IndexedDB 在**本机 WebView/浏览器**；换机或清数据后空 |
| 波特率 | 运行手册默认 **500000**；旧章节若写 921600 以固件与启动参数为准 |

---

## 16. 未来扩展路线

### 16.1 硬件与固件

- [ ] 串口自动探测与波特率统一文档化  
- [x] Wi‑Fi `upload_pcm` 生产路径（iPad 接收）  

### 16.2 Bridge / 服务

- [ ] 录完自动写本地 `.wav` 供调试  
- [x] Python / iPad 同契约 `:8001`  

### 16.3 前端

- [x] Library 样本 IndexedDB 持久化  
- [x] 色盘 Plate ≤5 多 brush  
- [x] Collect / New Sounds / My sounds / Gallery 产品面  
- [x] My Gallery 点击续画并覆盖保存  
- [ ] 录完可选「立即 Transform」  

### 16.4 视觉与 AI / iPad

- [x] Qwen 灰度五类语义定 `strokePattern`  
- [x] iPad 独立网关 + 内嵌 web-demo + Supabase anon  
- [ ] 笔刷参数与声学轴进一步调参  
- [ ] 多样本混音材料  

---

## 17. 修改记录（Changelog）

> **维护规则：** 每次功能、界面、固件或协议变更后，在本节**顶部**追加一条记录（最新在上）。  
> 同步更新文首「版本号 / 更新日期」，并在对应章节修正正文。  
> 格式：`日期 · 版本 · 摘要 → 涉及文件`

---

### 2026-08-07 · v1.6 — iPad 独立运行时 + Gallery 续画

| 项 | 内容 |
|----|------|
| **摘要** | 生产拓扑改为 iPad `LocalPikoGateway` + 内嵌 web-demo；文档对齐 Collect/New Sounds/Gallery；My Gallery 点击画作进入画板续画，保存覆盖同一 id；桌面 USB 标为开发路径 |
| **涉及文件** | `ios/PikoShell/**`、`web-demo/app.js`、`galleryStore.js`、`pikoGalleryScreen.js`、`pikoCanvasScreen.js`、`canvasInteraction.js`、`soundsApiClient.js`、`交互基准.md`、`技术说明书.md` |
| **备注** | 见 §3.1–3.2、§4、§12；密钥文件不入库 |

---

### 2026-07-18 · v1.5 — Qwen 定类 + 本地类内笔刷

| 项 | 内容 |
|----|------|
| **摘要** | 笔刷算法由 Qwen 五类决定；本地 `acousticFeatures` 仅在 `BRUSH_RANGES` 内调参；废弃本地定类驱动 `strokePattern` |
| **涉及文件** | `acoustic_features.py`、`brush_mapper.py`、`analysis_export.py`、`app.py`、`brushGenerator.js`、`BRUSH_ACOUSTIC_MAPPING.md`、`README.md`、`技术说明书.md` |
| **备注** | 见 §3.2、§8.4 |

---

### 2026-07-16 · v1.4 — 产品流文档对齐

| 项 | 内容 |
|----|------|
| **摘要** | 说明书对齐 Collect→Transform→Draw、Plate≤5、IndexedDB；当时仍写「Qwen 不改 strokePattern」（已被 v1.5 取代） |
| **涉及文件** | `README.md`、`技术说明书.md`、`CHANGELOG.md` |
| **备注** | UI PRD 文件已删除 |

---

### 2026-07-15 · v1.3 — SiliconFlow Qwen3-Omni 语义分析

| 项 | 内容 |
|----|------|
| **摘要** | `/analyze/wav` 接入 Qwen；后续演进为灰度五类 `semantic`（见 v1.4 / `siliconflow_omni.py`） |
| **涉及文件** | `python-service/siliconflow_omni.py`、`app.py`、`schemas.py`、`config.py`、`.env.example` |
| **备注** | 见 §8.4；Key 仅服务端 `.env` |

---

### 2026-06-17 · v1.2 — ESP32 硬件 PCM 录音（取代电脑麦克风）

| 项 | 内容 |
|----|------|
| **摘要** | Library 录音改为 **ESP32 + INMP441 硬件 PCM**：Start → 串流 PCM → Stop → 前端拼 WAV 保存 |
| **固件** | `inmp441_bridge.ino`：`REC_START`/`REC_STOP` 控制；921600 波特率；二进制 PCM 帧 `0xA5 0x5A 0x01` |
| **Bridge** | 解析 PCM 二进制帧；WebSocket `{cmd:"record_start/stop"}`；转发 `{type:"pcm", data: base64}` |
| **前端** | `fieldRecorder.js` 仅硬件路径；`wavEncoder.js` 拼 16kHz WAV；样本 `source: "esp32-record"` |
| **涉及文件** | `esp32/inmp441_bridge.ino`、`bridge/serial-bridge.js`、`fieldRecorder.js`、`esp32AudioAdapter.js`、`wavEncoder.js` |
| **必须** | 重新烧录固件 + Bridge 使用 `--baud 921600`；**不可**再用仅输出「音量%」的旧 sketch 录音 |

---

### 2026-06-17 · v1.1 — 补充修改记录章节

| 项 | 内容 |
|----|------|
| **摘要** | 在技术说明书中新增 §17 修改记录，建立持续更新机制 |
| **涉及文件** | `技术说明书.md` |
| **备注** | 回溯记录 v1.0 开发周期内全部已知变更 |

---

### 2026-06-17 · v1.0 — 移除文件上传入口

| 项 | 内容 |
|----|------|
| **摘要** | 删除「Import audio file instead」按钮及隐藏上传区；Library 仅保留现场录音 |
| **涉及文件** | `web-demo/index.html`、`web-demo/app.js`（移除 `setupFileUpload` / `handleFiles`）、`web-demo/fieldRecorder.js` |
| **原因** | Demo 预设为儿童在自然中现场采集，不需要上传文件 |

---

### 2026-06-17 · v1.0 — 现场录音采集流程（Collect）

| 项 | 内容 |
|----|------|
| **摘要** | Library 主流程改为：Start recording → Stop → 命名 → Save；一次开始–结束 = 一个样本 |
| **涉及文件** | `web-demo/fieldRecorder.js`（新建）、`web-demo/index.html`、`web-demo/app.js`、`web-demo/style.css` |
| **行为** | 工作流改为 ① Collect → ② Transform → ③ Draw；样本标记 `source: "field-record"`；录音电平 ESP32 优先 |
| **备注** | 录音文件暂由浏览器 `MediaRecorder` 生成（WebM/Opus） |

---

### 2026-06-17 · v1.0 — Bridge 兼容 Legacy 固件文本

| 项 | 内容 |
|----|------|
| **摘要** | 串口桥接支持 `sketch_jun16a` 中文输出（`音量: XX%`、`检测到声音` 等），映射为 level/peak/mean |
| **涉及文件** | `bridge/serial-bridge.js` |
| **原因** | 用户实际烧录固件与 JSON 版 `inmp441_bridge.ino` 不一致 |

---

### 2026-06-17 · v1.0 — ESP32 实时音频管线

| 项 | 内容 |
|----|------|
| **摘要** | 完整链路：ESP32 → USB Serial → Node Bridge → WebSocket → 前端可视化 |
| **新建** | `esp32/inmp441_bridge.ino`、`bridge/serial-bridge.js`、`bridge/package.json`、`web-demo/esp32AudioAdapter.js`、`README.md` |
| **修改** | `web-demo/app.js`（初始化 adapter）、`generativeField.js`、`brushGenerator.js`、`audioAnalyzer.js`、`index.html`、`style.css`（`#esp32Status`） |
| **映射** | level → 粒子密度/体积；peak → 闪烁；mean → 漂移/噪声偏移 |
| **协议** | WebSocket `ws://localhost:8765`；JSON `{ level, peak, mean, timestamp }` |

---

### 2026-06-17 · v1.0 — Transform 波形改为滚动示波器

| 项 | 内容 |
|----|------|
| **摘要** | Step1 波形由静态「已播/未播 + 播放头」改为 Web Audio 解码采样驱动的水平滚动示波器 |
| **涉及文件** | `web-demo/soundTransformView.js` |
| **后续微调** | 去掉人为上下起伏（`floatY`）避免头晕；去掉拖尾叠影，每帧全清 + 单线绘制 |

---

### 2026-06-17 · v1.0 — Sound Transform 透明管线页

| 项 | 内容 |
|----|------|
| **摘要** | 新增 Transform 视图：Recognize → 音量 → FFT → 音色 → 映射 → Sound Breathing 预览 |
| **涉及文件** | `web-demo/soundTransformView.js`（新建）、`web-demo/index.html`（`#transformView`）、`web-demo/style.css`、`web-demo/app.js`（Library → Transform → Analysis 路由） |
| **备注** | 分析使用 Web Audio + `SampleAnalyzer`（非 p5 FFT） |

---

### 2026-06-17 · v1.0 — 呼吸画笔（Brush = Sound Breathing）

| 项 | 内容 |
|----|------|
| **摘要** | 中央画布笔触改为与 GenerativeField 同源的粒子簇动画；拖拽即 spawn 活体材料 |
| **涉及文件** | `web-demo/brushGenerator.js`、`web-demo/canvasInteraction.js`、`web-demo/visualTransformation.js` |

---

### 2026-06-17 · v1.0 — Sound Breathing 粒子场优化

| 项 | 内容 |
|----|------|
| **摘要** | 左侧 sound breathing：离屏层渲染、透明背景、`destination-out` 拖尾、Perlin 噪声流、音量/FFT 映射、点描美学 |
| **涉及文件** | `web-demo/generativeField.js` |
| **修复** | 去除纸色半透明填充（解决红黄/奶油色光晕）；提高粒子密度与拖尾参数 |
| **约束** | 不改外层 canvas 布局与 DOM 位置 |

---

### 版本对照表

| 版本 | 日期 | 里程碑 |
|------|------|--------|
| v1.6 | 2026-08-07 | iPad 独立运行时；Gallery 续画；产品面文档对齐 |
| v1.5 | 2026-07-18 | Qwen 定 `strokePattern`；本地声学类内 `brushParams` |
| v1.4 | 2026-07-16 | Plate/IndexedDB 文档对齐（笔刷职责描述已被 v1.5 取代） |
| v1.3 | 2026-07-15 | SiliconFlow Qwen3-Omni 语义分析（`semantic`） |
| v1.2 | 2026-06-17 | ESP32 硬件 PCM 录音 |
| v1.1 | 2026-06-17 | 技术说明书 + 修改记录机制 |
| v1.0 | 2026-06-17 | ESP32 管线、现场录音、Transform 页、Sound Breathing/Brush 体系 |

---

## 附录 A：脚本加载顺序（index.html）

```html
p5.js → p5.sound → soundPersonalityAI → visualMapping → visualTransformation
→ generativeField → sampleAnalyzer → soundTransformView → brushGenerator
→ canvasInteraction → fieldRecorder → audioAnalyzer → esp32AudioAdapter
→ app.js → sketch.js
```

## 附录 B：关键类关系

```
App
 ├── FieldRecorder        (Library 录音)
 ├── SoundTransformView   (Transform 管线)
 ├── esp32AudioAdapter    (ESP32 实时)
 └── canvasInteraction    (p5 画布)
      ├── GenerativeField (左)
      ├── BrushGenerator  (中)
      └── VisualTransformation (顶)

SampleAnalyzer → SoundPersonalityAI → VisualMappingEngine
                      ↓
              generativeField / brushGenerator
```

---

**文档维护：** 功能变更时请同步更新本文档对应章节，并在 [§17 修改记录](#17-修改记录changelog) **顶部**追加一条。  
**快速上手：** `README.md` · **交互契约：** `交互基准.md` · **iPad：** `ios/PikoShell/README.md`。
