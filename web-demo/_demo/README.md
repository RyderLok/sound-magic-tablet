# TEMP demo only — safe to delete this whole folder

Does **not** change product source. Shows **5 brush styles** with distinct colors.

## Brush map (forced after Transform, runtime only)
1. Scatter Points — warm orange
2. Flow Field — teal/green
3. Wave Ripple — blue/cyan
4. Impact Burst — earth brown
5. Pulse Grid — lime green

Uses real WAVs from `python-service/output/`.

## Run
```bash
./web-demo/_demo/run.sh
```
Then: select **all 5** → Next → Transform → Sound Brush (switch rows) → Use Brush → Draw.

## Delete
```bash
rm -rf web-demo/_demo
```
